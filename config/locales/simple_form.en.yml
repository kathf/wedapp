en:
  simple_form:
    "yes": 'Yes'
    "no": 'No'
    required:
      text: 'required'
      mark: '*'
      # You can uncomment the line below if you need to overwrite the whole required html.
      # When using html, text and mark won't be used.
      # html: '<abbr title="required">*</abbr>'
    error_notification:
      default_message: "There were some problems with your form :("
    labels:
      defaults:
        name: 'Guest name'
        email: "Email"
    placeholders:
      defaults:
        name: "eg. Lionel Richie"
        email: "is_it_me_youre_looking_for@hello.com"
    # hints:
    #   defaults:
    #     email: '(optional)'
